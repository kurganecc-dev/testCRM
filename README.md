# ANDROMEDA modular split

Разнесено из одного HTML-файла:

- `index.html` — разметка и подключения
- `assets/css/styles.css` — стили и темы
- `assets/js/*.js` — логика по блокам в порядке исходного файла

Логика не переписывалась: порядок подключения сохранён, чтобы снизить риск регрессий.
